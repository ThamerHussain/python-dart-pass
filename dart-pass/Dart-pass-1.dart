main() {
  var hour = DateTime.now().hour;
  print(hour);
}

/*
we should specify the data type and since the time is variable then we can set 'var' or 'int' or 'num'
we shouldn't set 'const' to a variable data like time 
*/
